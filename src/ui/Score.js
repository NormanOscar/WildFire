class Score extends rune.text.BitmapField {
    constructor(score) {
        super(score);
    }

    init() {
        super.init();
        this.autoSize = true;
    }

    update(step) {
        super.update(step);
    }
    
    dispose() {
        super.dispose();
    }
}